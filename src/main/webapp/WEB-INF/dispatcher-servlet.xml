<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 添加扫描包，让注解生效 -->
    <context:component-scan base-package="com.igeek.ssm.controller , com.igeek.ssm.service"></context:component-scan>

    <!-- conversion-service="conversionService" -->
    <!-- 配置处理器映射器、处理器适配器 -->
    <!-- 校验器注入到处理器适配器中 -->
    <mvc:annotation-driven
        validator="validator"></mvc:annotation-driven>

    <!-- 配置视图解析器 -->
    <!--<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value=""></property>
        <property name="suffix" value=".jsp"></property>
    </bean>-->

    <!-- 自定义参数绑定 -->
    <!--<bean id="conversionService"
          class="org.springframework.format.support.FormattingConversionServiceFactoryBean">
        &lt;!&ndash; 转换器 &ndash;&gt;
        <property name="converters">
            <list>
                &lt;!&ndash; 日期类型转换 &ndash;&gt;
                <bean class="com.igeek.ssm.controller.CustomDateConverter"></bean>
            </list>
        </property>
    </bean>-->

    <!-- 校验器：spring提供的一个校验的接口LocalValidatorFactoryBean -->
    <bean id="validator" class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean">
        <!-- 校验器的提供方为hibernate的HibernateValidator -->
        <property name="providerClass" value="org.hibernate.validator.HibernateValidator" />
        <!-- 指定校验使用的资源文件，在文件中配置校验时的错误信息，如果不指定则默认使用classpath下的ValidationMessages.properties -->
        <property name="validationMessageSource" ref="messageSource" />
    </bean>

    <!-- 校验错误信息配置文件 -->
    <bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <!-- 资源文件名-->
        <property name="basenames">
            <list>
                <!-- basenames 可以不加后缀.properties
                CustomValidationMessages.properties文件中添加校验时错误信息-->
                <value>classpath:CustomValidationMessages</value>
            </list>
        </property>
        <!-- 资源文件编码格式 -->
        <property name="fileEncodings" value="utf-8" />
        <!-- 对资源文件内容缓存时间，单位秒 -->
        <property name="cacheSeconds" value="120" />
    </bean>

    <!-- 配置自定义的全局异常处理器 -->
    <!--<bean class="com.igeek.ssm.exception.MyHandlerExceptionResolver"></bean>-->

    <!-- 文件上传，配置multipart类型解析器 -->
    <bean id="multipartResolver"
          class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <!-- 设置上传文件的最大尺寸为5MB -->
        <property name="maxUploadSize">
            <value>5242880</value>
        </property>
    </bean>

    <!-- 对静态资源放行 -->
    <mvc:default-servlet-handler />

    <!-- 静态资源解析，包括：js、css、img等资源 -->
    <!--<mvc:resources location="/js/" mapping="/js/**"/>
    <mvc:resources location="/css/" mapping="/css/**"/>
    <mvc:resources location="/img/" mapping="/img/**"/>-->

    <!-- 配置拦截器 -->
    <mvc:interceptors>
        <!-- 根据配置顺序，来执行拦截器 -->
        <mvc:interceptor>
            <!-- /**代表对所有的路径都进行拦截处理 -->
            <mvc:mapping path="/**"/>
            <bean class="com.igeek.ssm.interceptor.MyInterceptor1"></bean>
        </mvc:interceptor>

        <mvc:interceptor>
            <!-- 设置拦截的路径 -->
            <mvc:mapping path="/**"/>
            <!-- 设置放行的路径 -->
            <!--<mvc:exclude-mapping path=""/>-->
            <bean class="com.igeek.ssm.interceptor.MyInterceptor2"></bean>
        </mvc:interceptor>

        <!-- 登录拦截器 -->
        <!--<mvc:interceptor>
            <mvc:mapping path="/**"/>
            <bean class="com.igeek.ssm.interceptor.LoginInterceptor"></bean>
        </mvc:interceptor>-->
    </mvc:interceptors>

</beans>